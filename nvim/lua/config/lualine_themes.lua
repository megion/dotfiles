function ChangeLualineTheme(theme_name)
  -- Setup lualine with the new theme
  -- require("lualine").setup({
  --   options = { theme = theme_name },
  -- })
end

-- Make the function globally accessible
_G.ChangeLualineTheme = ChangeLualineTheme
