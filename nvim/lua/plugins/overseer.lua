return {
  {
    "stevearc/overseer.nvim",
    ---@module 'overseer'
    ---@type overseer.SetupOpts
    opts = {},
    config = function(_, opts)
      -- require("overseer").setup()
    end,
  },
}
